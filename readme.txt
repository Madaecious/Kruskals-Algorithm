* To run the code simply open the file in an IDE and click run.

Note:
* The first line on the input.txt is the number of nodes in the graph
* The rest of the lines are the in the format:

	from_node, to_node, edge-weight